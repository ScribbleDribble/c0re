#include "../drivers/screen.h"

int main() {
	int row = 0;
	int col  = 0;
	
//	for (; row < MAX_HEIGHT; row++)
//		for (; col < MAX_WIDTH; col++)
//			kprint_char('z', 0x0f, row, col);
	//clear_screen();
}
		
